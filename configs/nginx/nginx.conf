upstream web {
    ip_hash;
    server accounting-py:8000;
}

# portal
server {
    location / {
        proxy_pass http://accounting-py/;
    }
    location /static/ {
        autoindex on;
        alias /dist/;
    }
    listen 8000;
    server_name localhost;
}